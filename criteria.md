# Scoring Criteria

## Top10 정렬

```text
score = clamp(Final_Base_Score + Chapter10_Adj + Chapter11_Adj + Chapter12_Adj, 0, 100)
Final_Base_Score = average(Base_Score_Daewoon_1, Base_Score_Daewoon_2, Base_Score_Daewoon_3, Base_Score_Daewoon_4)
```

- 계산 위치: `lib/lucky-days.ts`
- 후보 생성: 최대 3일 선택 기간의 날짜별 12개 시간대
- 정렬: `score desc`, `date asc`, `hour asc`
- 9장 용신은 산출/표시만 한다. 점수 가감은 6장 대운 점수 구현 때 연결한다.

## 입력 보정

- 성별:
  - `M`: 남아
  - `F`: 여아
  - `calculateSaju()`에 그대로 전달하며 대운 방향과 10장 성별 가감점에 반영한다.
- 출산 지역:
  - 사용자는 지역명을 텍스트로 입력한다.
  - 입력값은 `lib/birth-options.ts`의 내부 지역/별칭 테이블로 매칭한다.
  - 예: `강남구` -> 서울, `해운대구` -> 부산.
  - 매칭 실패 시 서울 기준으로 fallback하고 `matched=false`를 응답한다.
  - 지역별 위도/경도는 `lib/birth-options.ts`에 정의한다.
  - 기준 경도는 동경 `135.0도`.
  - 시간 보정:

```text
correctionMinutes = round((location.longitude - 135.0) * 4)
adjustedBirthTime = selectedLocalBirthTime + correctionMinutes
```

- 예: 서울 `126.978도` -> `-32분`, 부산 `129.0756도` -> `-24분`.
- 사주 계산에는 보정된 날짜/시/분을 입력한다.
- 후보 카드의 날짜/시간은 사용자가 선택한 출산 예정 현지 시각을 유지하고, 상세에는 보정 계산시각을 표시한다.

## 7장 오행 기도

- 일간 천간 제외.
- 천간 기본값: `0.7`
- 천간 가중치:
  - 월지 통근 `+0.20`
  - 일지 통근 `+0.14`
  - 연지/시지 통근 각 `+0.08`
  - 좌하 비겁 `+0.05`
  - 좌하 인성 `+0.02`
  - 좌하 극 `-0.05`
  - 좌하 설기 `-0.02`
  - 인접 천간 충 `-0.05`
  - 인접 천간 합화/합반 `-0.05`
  - 합화 성립 시 합화 오행에 `0.7 * 0.20` 추가
- 지지:
  - 지장간 월률 비율 사용
  - 월지 사령 `+0.15`
  - 일지 안착 `+0.08`
  - 인접 지지 충 `-0.05`
  - 삼합 왕지 `+0.20`
  - 반합 왕지 `+0.08`
  - 삼합/반합 생지·고지 `-0.10`
- 산출:
  - `totals`: 목/화/토/금/수 원 기도 총량
  - `percentages`: 총량 기준 정규화 퍼센트
  - `breakdown`: 개별 천간/지장간 산출 행

## 8장 SI / Base_Score

역할 매핑:

- 비겁: 일간과 같은 오행
- 인성: 일간을 생하는 오행
- 식상: 일간이 생하는 오행
- 재성: 일간이 극하는 오행
- 관성: 일간을 극하는 오행

```text
생조 = 인성 + 비겁 * 1.1
극설 = 식상 + 재성 + 관성 * 1.1
SI = (생조 - 극설) / (생조 + 극설) * 100
Base_Score = 100 - abs(17.5 - SI) * K
```

K 매핑:

| 구간 | 조건 | K |
| --- | --- | ---: |
| 극신강 | `SI > 30` | `1.50` |
| 약신강 | `5 < SI <= 30` | `0.18` |
| 중화 | `-5 <= SI <= 5` | `0.40` |
| 약신약 | `-30 <= SI < -5` | `0.44` |
| 극신약 | `SI < -30` | `0.90` |

- `Base_Score`는 소수점 둘째 자리 반올림 후 `0~100` 제한.
- 후보 점수에 쓰는 `Final_Base_Score`는 1~4대운 각각을 원국 오행 기도에 추가해 산출한 `Base_Score_Daewoon_i`의 산술 평균이다.
- 대운 천간은 위치 가중치 없이 천간 기본값 `0.7`로 반영한다.
- 대운 지지는 위치 가중치 없이 지장간 월률 비율을 반영한다.
- 대운은 위치 관계, 인접 충합, 월지/일지 안착 가중치를 적용하지 않는다.
- 원국 `Base_Score`는 상세 근거로 표시하고, 정렬/최종 점수에는 `Final_Base_Score`를 사용한다.

## 9장 용신

조후 붕괴:

- 화만 존재 또는 수만 존재하고 존재 오행 기도 `>= 20%`
- 화/수 모두 존재하고 `max(火, 水) >= 20%` 및 `min/max <= 1/3`

용신 결정:

- 조열: 용신 `水`
- 한랭: 용신 `火`
- 조후 중화 + `SI >= 0`: 식상/재성/관성 중 기도 비율 최대 역할의 오행
- 조후 중화 + `SI < 0`: 인성/비겁 중 기도 비율 최대 역할의 오행

대표 글자:

- 7장 `breakdown`에서 용신 오행 후보 수집.
- 후보 없음: 용신을 생하는 희신 오행 후보 수집.
- 정렬:
  1. 기도값 내림차순
  2. 위치 우선순위: 월지 > 일지 > 시지 > 월간 > 시간 > 연간 > 연지
  3. 양간 우선

## 10장 십성 가감점

십성 역할:

- 비겁: `比肩`, `劫財`, `本元`
- 식상: `食神`, `傷官`
- 재성: `偏財`, `正財`
- 관성: `偏官`, `正官`
- 인성: `偏印`, `正印`

가감점:

| 조건 | 점수 |
| --- | ---: |
| 원국 천간/지지에 오행 5개 모두 존재 | `+2.0` |
| 식상·재성·관성 모두 존재 | `+2.0` |
| 편관과 식신 동시 존재 | `+2.0` |
| 상관과 관성 동시 존재 | `-1.0` |
| 편인과 식신 동시 존재, 재성 없음 | `-3.0` |
| 비겁 기도 `>= 40%`, 관성 없음 | `-2.0` |
| 성별 `F`, 관성 없음 | `-2.0` |
| 성별 `M`, 재성 없음 | `-2.0` |

## 11장 12운성 / 12신살 가감점

12신살:

- 일지 삼합 그룹 기준으로 별도 산출.
- 반안살 조건은 일지 또는 시지의 일지-기준 12신살이 `攀鞍`.

가감점:

| 조건 | 점수 |
| --- | ---: |
| 시지 12운성 `長生`, `乾祿`, `建祿`, `冠帶`, `衰` | `+0.5` |
| 일지 12운성 `長生`, `乾祿`, `建祿`, `衰` | `+0.5` |
| 일지 또는 시지 반안살 | `+0.5` |
| 시지 12운성 `絶`, `病`, `死` | `-0.5` |
| 일지 12운성 `沐浴`, `絶` | `-0.2` |

## 12장 길성 / 흉살 / 공망 가감점

가감점:

| 조건 | 점수 |
| --- | ---: |
| 천을귀인 일지/시지 | 개당 `+1.5` |
| 천을귀인 연지/月지 | 개당 `+1.0` |
| 재고귀인 일주: `甲辰`, `乙丑`, `丁丑`, `己丑`, `庚戌`, `辛未`, `壬戌` | `+1.0` |
| 연지/月지/시지에 `辰`, `戌`, `丑`, `未` | 개당 `+0.5` |
| 천덕 또는 월덕 보유 | `+0.2` |
| 문창 보유 | `+0.1` |
| 금여 보유 | `+0.2` |
| `寅巳申` 또는 `丑戌未` 모두 존재 | `-1.5` |
| 인접 `戌亥` 또는 `辰巳`, 그중 하나가 일지/月지 | `-1.0` |
| 일지-월지 또는 일지-시지 원진/귀문 | `-1.5` |
| 공망 지지의 십성이 `正官`, `正印`, `正財`, `偏財` | 개당 `-0.5` |
| 그 외 공망 지지 | 개당 `-0.2` |

## 미반영

- 6장 추천 제외 조건 게이트
- 6장 1~8대운 평균 최종 점수. 현재는 요청 범위에 맞춰 1~4대운 평균 Base Score만 반영한다.
- 대운 용희/십성/신살 가감점
- 분 단위 후보 생성. 현재 후보는 2시간 단위.
- 외부 지오코딩 API. 현재는 내부 지역/별칭 테이블로 처리한다.
