# 택일 점수 로직, v1.0 현행 구현

이 문서는 `lib/lucky-days.ts`, `lib/saju/element-qi.ts`, `lib/saju/strength-index.ts`, `lib/saju/base-score.ts`의 실제 동작을 요약한다. 전통 명리 해석을 정량화한 서비스 내부 비교 점수이며, 의학적 안전성이나 출산 일정 결정을 대신하지 않는다.

## 1. 후보 생성과 순위

- 선택 기간은 2일 이상, 최대 3일이다.
- 하루를 12개 시진, 00시부터 2시간 간격으로 나누므로 최대 36개 후보를 평가한다.
- 지역 중심 경도에 따라 `보정분 = round((경도 - 135) × 4)`를 적용한다. 예를 들어 광안리 129.1102도는 -24분, 서울특별시 중심은 -32분이다.
- 보정 태양시를 사주 엔진에 넣고, 엔진의 시진 경계 차이를 맞추기 위해 계산 입력에만 30분을 더한다.
- 최종 정렬은 표시 점수 내림차순, 상한 적용 전 원점수 내림차순, 날짜 오름차순, 시간 오름차순이며 상위 3개를 노출한다.

예시 A는 서울, 남아, 2026-09-01 02:00 후보로 `丙午년 丙申월 戊寅일 癸丑시`이다. 같은 날짜의 예시 C `甲寅시`와 날짜는 같아도 시주가 달라져 오행 비중, 신강신약, 길성 가감점이 달라진다.

## 2. 오행 비중과 기본 점수

일간을 제외한 천간은 기본값 0.7, 모든 지지는 지장간 비율을 기본값으로 사용한다. 천간에는 월지 통근 +20%, 일지 통근 +14%, 연지, 시지 통근 각각 +8%, 좌하 관계 +5%, +2%, -5%, -2%, 인접 천간 충, 합 각각 -5%가 적용된다. 합화가 성립하면 합화 오행에 `0.7 × 20%`를 추가한다. 지지에는 월지 +15%, 일지 +8%, 인접 충 -5%, 삼합 왕지 +20%, 삼합 생지, 고지 -10%, 반합 왕지 +8%, 반합 생지, 고지 -10%가 적용된다.

오행 비중을 일간 기준 십성 역할로 바꾼 뒤 다음 식을 쓴다.

```text
생조 = 인성% + 비겁% × 1.1
극설 = 식상% + 재성% + 관성% × 1.1
SI = (생조 - 극설) / (생조 + 극설) × 100
Base Score = clamp(100 - |17.5 - SI| × K, 0, 100)
```

| SI 구간 | 판정 | K |
|---|---|---:|
| SI > 30 | 극신강 | 1.50 |
| 5 < SI ≤ 30 | 약신강 | 0.18 |
| -5 ≤ SI ≤ 5 | 중화 | 0.40 |
| -30 ≤ SI < -5 | 약신약 | 0.44 |
| SI < -30 | 극신약 | 0.90 |

예시 A는 SI 23.80, 약신강이므로 원국 Base Score가 `100 - |17.5 - 23.8| × 0.18 = 98.87`이다. 예시 C는 SI 15.81로 최적점 17.5에 더 가까워 원국 Base Score가 99.70으로 더 높다.

## 3. 대운 평균 Base Score

1대운부터 8대운까지 각 대운의 천간 0.7과 지지 지장간 비율을 원국 오행에 더하고 SI와 Base Score를 다시 계산한다. 여덟 점수의 단순 평균이 최종 계산의 기본값이며, 대운이 없을 때만 원국 Base Score를 사용한다.

```text
대운 평균 Base = (1대운 Base + ... + 8대운 Base) / 8
원점수 = 대운 평균 Base + 10장 가감 + 11장 가감 + 12장 가감
표시 점수 = clamp(원점수, 0, 100)
```

예시 A는 원국 98.87이지만 대운 평균은 94.80625이다. 예시 C는 원국 99.70, 대운 평균 95.72875로 A보다 기본 흐름이 0.9225점 높다.

## 4. 십성, 구조 가감점

| 조건 | 점수 | 예시와 비교 |
|---|---:|---|
| 목, 화, 토, 금, 수 모두 존재 | +2 | A는 오행 구족으로 +2, C는 미충족으로 0 |
| 식상, 재성, 관성이 모두 존재 | +2 | A와 C 모두 성립해 +2 |
| 편관과 식신이 함께 존재 | +2 | A와 C 모두 식신제살로 +2 |
| 상관과 정관 또는 편관이 함께 존재 | -1 | A는 상관견관으로 -1, C는 0 |
| 편인과 식신이 있고 재성이 없음 | -3 | 성립 후보만 편인도식 -3 |
| 비겁 비중이 40% 이상이고 관성이 없음 | -2 | 비겁 과다 후보를 균형 후보보다 -2 |
| 여아인데 관성이 없음 | -2 | 동일 사주도 여아 입력일 때만 적용 |
| 남아인데 재성이 없음 | -2 | 동일 사주도 남아 입력일 때만 적용 |

십성 존재 여부는 천간 십성, 지지 십성, 지장간 좌법 십성을 합쳐 판단한다.

## 5. 12운성, 신살 가감점

| 조건 | 점수 |
|---|---:|
| 시지 12운성이 장생, 건록, 관대, 쇠 | +0.5 |
| 일지 12운성이 장생, 건록, 쇠 | +0.5 |
| 일지 또는 시지에 반안살 | +0.5 |
| 시지 12운성이 절, 병, 사 | -0.5 |
| 일지 12운성이 목욕, 절 | -0.2 |

예시 A는 일지 길성 배치로 +0.5를 받는다. 예시 C는 시지와 일지에서 각각 +0.5를 받아 이 영역에서 A보다 0.5점 높다.

| 길성, 흉살 조건 | 점수 |
|---|---:|
| 천을귀인 일지, 시지 | 개당 +1.5 |
| 천을귀인 연지, 월지 | 개당 +1.0 |
| 재고귀인 일주 | +1.0 |
| 연지, 월지, 시지의 辰, 戌, 丑, 未 | 개당 +0.5 |
| 천덕귀인 또는 월덕귀인 보유 | +0.2, 중복이어도 1회 |
| 문창귀인 보유 | +0.1 |
| 금여록 보유 | +0.2 |
| 寅巳申 또는 丑戌未 삼형살 완성 | -1.5 |
| 일지, 월지를 포함한 인접 戌亥 또는 辰巳 | -1.0 |
| 일지와 월지, 시지 사이 원진 또는 귀문 | -1.5 |
| 공망 지지의 십성이 정관, 정인, 정재, 편재 | 기둥마다 -0.5 |
| 그 밖의 십성이 공망 | 기둥마다 -0.2 |

예시 A는 천을귀인 +1.5, 타지 재고 +0.5, 천덕, 월덕 +0.2, 문창 +0.1, 기타 십성 공망 -0.2로 이 영역 합계 +2.1이다. 예시 C는 문창 +0.1, 기타 십성 공망 -0.2로 합계 -0.1이므로 A가 2.2점 높다.

## 6. 실제 계산 예시와 개선 포인트

| 후보 | 대운 평균 Base | 전체 가감 | 원점수 | 표시 점수 |
|---|---:|---:|---:|---:|
| A, 癸丑시 | 94.80625 | +7.60 | 102.41 | 100.0 |
| B, 壬戌시 | 94.54875 | +6.10 | 100.65 | 100.0 |
| C, 甲寅시 | 95.72875 | +4.90 | 100.63 | 100.0 |

세 후보 모두 화면에는 100점으로 보이지만 순위는 원점수 A, B, C 순으로 구분된다. 현재 개선 우선순위는 다음과 같다.

1. 100점 포화가 잦으므로 가감점 배율 또는 Base Score 중심값을 실제 후보 분포로 재보정한다.
2. 용신과 조후는 계산하고 설명하지만 현재 점수 기여도는 0점이다. 오행 비중도 별도 점수를 더하지 않고 Base Score 입력으로만 사용한다.
3. 기존 요구 문서의 치명 조건 즉시 제외, 대운별 용희신, 십성, 신살 가감은 아직 구현되지 않았다. 현재는 원국 가감점을 대운 평균 Base에 한 번만 더한다.
4. 성별 무재, 무관은 즉시 제외가 아니라 -2점이므로 정책을 확정한 뒤 제외 규칙과 감점 규칙 중 하나로 통일해야 한다.

점수 정책을 바꿀 때는 후보 전체의 원점수 분포, 100점 비율, 성별별 추천 변화, 같은 날짜 내 시간대 순위 변화를 회귀 테스트해야 한다.
