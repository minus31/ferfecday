# Scoring Progress

## 현재 범위

- 집중 범위: 주어진 사주 후보의 원국 scoring.
- 보류 범위: 6장 최종 대운 평균 점수, 분 단위 후보 생성.
- 현재 점수는 브라우저에서 `lib/lucky-days.ts`를 실행해 산출한다. 8장 Base_Score에 10-12장 가감점을 더한 원국 scoring 기준으로 Top10 정렬에 반영한다.

## 완료 / 진행 중

### 7장. 오행 기도 비율

- 구현 위치: `lib/saju/element-qi.ts`.
- 일간 천간은 천간 기도 계산에서 제외한다.
- 천간 기본 기도값 `0.7`을 적용한다.
- 월지/일지/연지/시지 통근 가중치를 적용한다.
- 좌하 비겁 득세, 인성 득세, 좌하 극, 좌하 설기를 적용한다.
- 인접 천간 충, 합화, 합반 가중치를 적용한다.
- 지지는 지장간 월률 비율로 분해한다.
- 월지 사령, 일지 안착, 지지 정면 충, 삼합 왕지, 반합 왕지, 생지/고지 변질 가중치를 적용한다.
- 목/화/토/금/수 기도 총량과 정규화 퍼센트를 산출한다.

### 8장 연계를 위한 7장 출력 정리

- `calculateElementQi()` 반환값:
  - 오행별 원 기도 총량
  - 오행별 정규화 퍼센트
  - 결핍 오행 목록
  - 개별 산출 근거 `breakdown`
- SI/Base_Score 구현에서 재사용할 공통 헬퍼를 추가했다:
  - `ELEMENT_KEYS`
  - `GENERATES`
  - `CONTROLS`
  - `getElement()`
  - `getDayMasterElement()`
  - `getElementRole()`
  - `getElementRoleQi()`
- `getElementRoleQi()`는 일간 오행 기준으로 각 오행 기도를 8장에 필요한 역할별 기도로 매핑한다:
  - `bigeop` / 비겁
  - `insung` / 인성
  - `siksang` / 식상
  - `jaeseong` / 재성
  - `gwanseong` / 관성

### 8장. 신강/신약 SI 및 Base_Score

- 구현 위치: `lib/saju/strength-index.ts`.
- 7장의 `calculateElementQi()` 결과를 입력으로 받아 일간 기준 역할별 기도를 산출한다.
- 생조기운 공식:
  - `인성 기도 + (비겁 기도 * 1.1)`
- 극설기운 공식:
  - `식상 기도 + 재성 기도 + (관성 기도 * 1.1)`
- SI 공식:
  - `(생조기운 - 극설기운) / (생조기운 + 극설기운) * 100`
- SI 구간별 판정과 K값을 적용한다:
  - 약신강: `5 < SI <= 30`, `K = 0.18`
  - 중화: `-5 <= SI <= 5`, `K = 0.40`
  - 약신약: `-30 <= SI < -5`, `K = 0.44`
  - 극신강: `SI > 30`, `K = 1.50`
  - 극신약: `SI < -30`, `K = 0.90`
- Base_Score 공식:
  - `100 - abs(17.5 - SI) * K`
  - 최종 반환값은 소수점 둘째 자리 반올림 후 `0~100`으로 제한한다.
- 후보 데이터에 `strength` 필드를 추가했다.
- 상세 화면의 `나의 오행·기도` 섹션에 신강/신약, SI, Base Score, 생조/극설, 역할별 기도를 표시한다.

### 9장. 하이브리드 조후/억부 용신

- 구현 위치: `lib/saju/yongshin.ts`.
- 화/수 기도 비율로 조후 붕괴 여부를 판정한다.
- 조열이면 수, 한랭이면 화를 조후 용신으로 확정한다.
- 조후가 중화이면 SI 기준으로 억부 용신을 확정한다.
  - `SI >= 0`: 식상/재성/관성 중 기도 비율이 가장 큰 역할 선택.
  - `SI < 0`: 인성/비겁 중 기도 비율이 가장 큰 역할 선택.
- 7장 `breakdown`에서 용신 오행 후보 글자를 모아 대표 용신 글자를 고른다.
- 동점은 월지 > 일지 > 시지 > 월간 > 시간 > 연간 > 연지, 이후 양간 우선으로 처리한다.
- 원국에 용신 오행이 없으면 용신을 생하는 희신 오행 후보를 fallback으로 사용한다.
- 후보 데이터와 상세 UI에 `yongshin` 결과를 추가했다.

### 10장. 십성 평가 모델

- 구현 위치: `lib/saju/base-score.ts`.
- 오행 구족, 식재관 유기통관, 식신제살 가점 구현.
- 상관견관, 편인도식, 비겁태과 무관, 성별 기반 무관/무재 감점 구현.
- 선택된 성별을 십성 가감점에 반영한다.

### 11장. 12운성 및 12신살

- 구현 위치: `lib/saju/base-score.ts`.
- 시지 길성, 일지 길성, 반안살 안착 가점 구현.
- 시지 무력, 일지 기복 감점 구현.
- 12신살은 문서 기준에 맞춰 일지 삼합 그룹 기준으로 별도 산출한다.

### 12장. 길성/흉살/공망

- 구현 위치: `lib/saju/base-score.ts`.
- 천을귀인, 재고귀인, 천덕·월덕, 문창, 금여록 가점 구현.
- 삼형살, 천라지망, 원진귀문, 길성 공망, 기타 십성 공망 감점 구현.

### Top10 반영

- `lib/lucky-days.ts`의 scoring pipeline이 새 원국 scoring을 사용한다.
- 최종 후보 점수는 `8장 Base_Score + 10장 가감점 + 11장 가감점 + 12장 가감점`을 `0~100`으로 제한한 값이다.
- Top10 정렬은 이 새 `score` 값을 기준으로 수행한다.
- 카드의 score contribution bar도 8-12장 구조 기준으로 변경했다.

### 입력 조건

- 기간 선택은 최대 3일로 제한한다.
- 성별 선택을 Home 화면에 추가했다.
  - `M`: 남아
  - `F`: 여아
  - `calculateSaju()`에 전달되어 대운 흐름과 성별 기반 십성 가감점에 반영된다.
- 출산 지역 텍스트 입력을 Home 화면에 추가했다.
- 입력 지역은 내부 지역/별칭 테이블로 매칭한다.
- 매칭 실패 시 서울 기준으로 fallback하고 응답에 `matched=false`를 포함한다.
- 지역별 위도/경도와 시간 보정 헬퍼는 `lib/birth-options.ts`에 둔다.
- 동경 135도 기준 보정 공식:
  - `correctionMinutes = round((longitude - 135.0) * 4)`
- 사주 계산에는 보정된 날짜/시/분을 입력한다.
- 후보 데이터에는 선택 지역, 성별, 보정 분, 보정 계산시각을 포함한다.

### Toss Mini App

- `@apps-in-toss/web-framework`를 설치했다.
- `npx ait init --app-name birthdaygift`로 웹 프레임워크 설정을 초기화했다.
- `granite.config.ts`를 추가했다.
  - `appName`: `birthdaygift`
  - `brand.displayName`: `생일선물`
  - `web.commands.dev`: `next dev`
  - `web.commands.build`: `next build`
  - `outdir`: `out`
- Next.js는 `output: "export"` 정적 export 모드로 전환했다.
- 서버 API Route를 제거하고 결과 계산을 클라이언트 실행 가능한 `lib/lucky-days.ts`로 이동했다.
- `npm run build`는 `ait build`를 실행해 `birthdaygift.ait` 아티팩트를 생성한다.
- 기존 Next 검증용 명령은 `npm run next:build`로 유지한다.

## 현재 남은 부분

- 6장 최종 점수 공식은 의도적으로 아직 구현하지 않았다.
- 6장 추천 제외 조건 게이트는 아직 구현하지 않았다.
- 분 단위 후보 생성은 아직 없다. 현재 후보는 2시간 단위다.
- scoring 수식 검증용 단위 테스트가 아직 없다.
- 같은 오행을 가진 일부 지장간은 오행 총량 관점에서 0 비율 행으로 사실상 접혀 있다. 대표 용신 글자 선정은 현재 7장 breakdown에 남는 후보 기준으로 동작한다.

## 다음 구현 순서

1. 6장 추천 제외 조건 게이트 구현.
2. 6장 전체 scoring system 업데이트.
3. 원국 scoring 안정화 후 대운에 따른 사주 점수 반영.
